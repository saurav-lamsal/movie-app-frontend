const Movie = ({ title }) => {
  return <h1>Lets Watch {title} movie</h1>;
};

export default Movie;

//props--> it is a way to pass data from parent component to child component
//--> props are always read-only (immutable)
